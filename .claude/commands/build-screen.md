Build the screen described in `design.md` for: $ARGUMENTS

1. Read the wireframe section for this screen in design.md — match layout, sticky/fixed elements, and stated priority (e.g. POS cart panel must stay sticky).
2. Use the design tokens from design.md (dark theme, colors, Inter + IBM Plex Mono for numbers).
3. Build mobile behavior as described (bottom tab bar, stacked cards, etc.) — not just desktop.
4. Implement all four states: loading (skeleton), empty (sentence + action button), error (inline, specific), success (toast, bottom-right, 3s).
5. Use ShadCN components where they fit; don't reinvent primitives that already exist in the library.

Show me the component tree before writing full code if the screen has more than 3 sub-components.
